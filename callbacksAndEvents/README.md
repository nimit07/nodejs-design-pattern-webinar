# 02 callbacks and Events

Examples to demonstrate synchronus & asynchronus cps, eventEmitter

## Run

Install the necessary dependencies with `npm install` and then run:

```bash
node callbacksAndEvents/<fileName>.js
```
